<view class="form">
  <view class="form-box">
    <view class="label">
      <view class="label-box">
        <text wx:if="{{data.important}}" class="important">*</text>
        <text class="label-info">{{data.label}}</text>
      </view>
    </view>
    <slot></slot>
  </view>
</view>